# Frozen gallery templates

Only empty, approved gallery environments belong here. Each image must have a
same-stem JSON sidecar conforming to `template.schema.json` and recording its
wall/floor calibration. Do not include artwork, visitors, or scale figures in a
template. Templates are frozen after approval so installation visualisations
remain visually consistent.

